framework:
    messenger:
        transports:
            async:
                dsn: doctrine://default

when@test:
    framework:
        messenger:
            transports:
                async: test://
                scheduler_default: test://
